1.  SELECT, WHERE, GROUP BY, HAVING
2.  All JOINs → INNER, LEFT, RIGHT, FULL, SELF
3.  UNION / UNION ALL
4.  Subqueries + Correlated Subqueries
5.  EXISTS / NOT EXISTS
6.  CASE WHEN
7.  NULL Handling → COALESCE, NULLIF, IS NULL
8.  String Functions → TRIM, REPLACE, SUBSTR,
           LEFT, RIGHT, LEN, UPPER, LOWER
9.  Date Functions → DATEDIFF, DATEADD,
                     EXTRACT, TO_CHAR, SYSDATE
10. CTEs → Single + Chained
11. Recursive CTEs 
12. Window Functions → ROW_NUMBER, RANK,
                       DENSE_RANK, NTILE
13. LAG / LEAD
14. SUM OVER / AVG OVER / COUNT OVER
15. FIRST_VALUE / LAST_VALUE
16. ROWS/RANGE BETWEEN
17. PIVOT / UNPIVOT
18. LATERAL JOIN 
19. Query Optimisation
    → Reading slow queries
    → Identifying bottlenecks
    → Rewriting for performance

20. EXPLAIN Plans
    → Understanding query execution
    → Cost estimation
    → Index usage analysis

21. Index Concepts
    → When to use indexes
    → Clustered vs non-clustered
    → Index on partition keys

22. Partition Pruning
    → How partitions speed queries
    → Writing partition-aware SQL
    → Redshift/Snowflake specific

23. Hard Mixed Problems
    → Combining multiple concepts
    → Real Meta/Salesforce style
    → Time pressure

